name: async_wallpaper_example
description: Demonstrates how to use the async_wallpaper plugin.
version: 3.2.0+1

publish_to: "none"

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  async_wallpaper:
    path: ../
  dynamic_color: ^1.8.1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
